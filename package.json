{
  "name": "@quenk/potoo",
  "version": "0.6.2",
  "description": "An actor model library for JavaScript.",
  "main": "lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "npm run -s prep:test; ./node_modules/.bin/mocha --opts mocha.opts test/func",
    "clean": "rm -R ./lib/*",
    "examples": "npm run -s prep:test && npm run -s build:examples ; ./node_modules/.bin/browserify --debug -o examples/examples.js examples/index.js",
    "build": "npm run -s clean; tsc --project src/tsconfig.json",
    "build:example": "tsc example/index.ts",
    "prep:test": "npm run build:lib; rm -R ./node_modules/potoo; ln -s $(pwd)/lib $(pwd)/node_modules/potoo;"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.20",
    "@types/mocha": "^2.2.48",
    "@types/node": "^8.9.2",
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "browserify": "^14.5.0",
    "fluture": "^5.0.0",
    "mocha": "^3.5.3",
    "must": "^0.13.4",
    "source-map-support": "^0.4.18",
    "ts-node": "^3.0.2",
    "typescript": "^2.7.1"
  },
  "author": "Quenk Technologies",
  "license": "Apache-2.0",
  "dependencies": {
    "@quenk/kindof": "^1.2.0",
    "@quenk/match": "^1.2.0",
    "afpl": "^0.12.0",
    "bluebird": "^3.5.1"
  }
}
